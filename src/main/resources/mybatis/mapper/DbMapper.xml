<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.roncoo.mysql.shard.mapper.DbMapper">
    <select id="executeQueryAtMaster" resultType="java.util.Map">
		${sql}
	</select>

    <select id="executeQueryAtSlave" resultType="java.util.Map">
		${sql}
	</select>
</mapper>